source "https://rubygems.org"

# Used to preview the theme itself locally via `bundle exec jekyll serve`.
# Consumers don't need this Gemfile — they set `remote_theme:` and use their own.
gem "jekyll", ">= 3.9", "< 5.0"
gem "jekyll-seo-tag"
gem "jemoji"
gem "jekyll-include-cache"
