source "https://rubygems.org"

# Used to preview the theme itself locally via `bundle exec jekyll serve`.
# Pulls in this gem's own runtime dependencies (jekyll, all the plugins) from
# jekyll-theme-giellalt.gemspec — the same set every consumer repo gets.
gemspec
